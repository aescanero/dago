# .env.example — copy to .env and fill in secrets before running `make compose-up`.
# Variables marked (required) have no safe default and MUST be set.

# ---------------------------------------------------------------------------
# Infrastructure
# ---------------------------------------------------------------------------
POSTGRES_USER=dago
POSTGRES_PASSWORD=dago
POSTGRES_DB=dago
POSTGRES_PORT=5432

VALKEY_PORT=6379

# ---------------------------------------------------------------------------
# orchestrator (HTTP :8080)
# ---------------------------------------------------------------------------
DATABASE_URL=postgres://dago:dago@postgres:5432/dago?sslmode=disable
PORT=8080
VALKEY_ADDR=valkey:6379

AUTH_REQUIRED=false
JWKS_URL=http://auth-server:8081/.well-known/jwks.json
JWT_ISSUER=dago-auth
JWT_AUDIENCE=dago-api

# ---------------------------------------------------------------------------
# executor (event-driven worker)
# ---------------------------------------------------------------------------
EXECUTOR_VALKEY_ADDR=valkey:6379
EXECUTOR_LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...
OLLAMA_BASE_URL=http://localhost:11434

# ---------------------------------------------------------------------------
# router (event-driven worker — stub)
# ---------------------------------------------------------------------------
ROUTER_VALKEY_ADDR=valkey:6379

# ---------------------------------------------------------------------------
# planner (event-driven worker — stub)
# ---------------------------------------------------------------------------
PLANNER_VALKEY_ADDR=valkey:6379

# ---------------------------------------------------------------------------
# auth-server (HTTP :8081)
# ---------------------------------------------------------------------------
AUTH_DATABASE_URL=postgres://dago:dago@postgres:5432/dago?sslmode=disable
AUTH_PORT=8081
JWT_PRIVATE_KEY_PATH=/secrets/jwt_private.pem

# ---------------------------------------------------------------------------
# catalog (HTTP :8082 — stub)
# ---------------------------------------------------------------------------
CATALOG_PORT=8082

# ---------------------------------------------------------------------------
# mcp-registry (HTTP :8083 — stub)
# ---------------------------------------------------------------------------
MCP_PORT=8083

# ---------------------------------------------------------------------------
# agent-registry (HTTP :8084 — stub)
# ---------------------------------------------------------------------------
AGENT_REGISTRY_PORT=8084

# ---------------------------------------------------------------------------
# dashboard build args
# ---------------------------------------------------------------------------
VITE_API_URL=http://localhost:8080
VITE_AUTH_URL=http://localhost:8081
VITE_AUTH_CLIENT_ID=dago-dashboard
VITE_AUTH_REDIRECT_URI=http://localhost:3000/auth/callback
